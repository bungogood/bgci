games = 100
seed = 42
max_plies = 512
swap_sides = true
variant = "backgammon"
log = "off"
output_csv = "artifacts/duels/latest/results.csv"

[engine_a]
name = "random-a"
command = ["cargo", "run", "--quiet", "--bin", "random_engine"]

[engine_b]
name = "random"
command = ["cargo", "run", "--quiet", "--bin", "random_engine"]
