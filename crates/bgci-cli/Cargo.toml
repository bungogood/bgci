[package]
name = "bgci-cli"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Reproducible backgammon engine testing over UBGI"
default-run = "bgci"

[[bin]]
name = "bgci"
path = "src/main.rs"

[dependencies]
bgci-core = { path = "../bgci-core" }
clap = { version = "4.5", features = ["derive"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
tracing = "0.1"
tracing-appender = "0.2"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
