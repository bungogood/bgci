games = 100
seed = 42
max_plies = 512
swap_sides = true
variant = "backgammon"
log = "info"

[engine_a]
name = "gnubg-cli"
command = ["cargo", "run", "--quiet", "--bin", "gnubg_engine"]

[engine_b]
name = "random"
command = ["cargo", "run", "--quiet", "--bin", "random_engine"]
