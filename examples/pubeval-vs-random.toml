games = 100
seed = 42
max_plies = 512
swap_sides = true
variant = "backgammon"
log = "info"

[engine_a]
name = "pubeval"
command = ["cargo", "run", "--release", "--quiet", "--bin", "pubeval_engine"]

[engine_b]
name = "random"
command = ["cargo", "run", "--release", "--quiet", "--bin", "random_engine"]
